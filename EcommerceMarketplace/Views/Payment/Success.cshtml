@{
    ViewData["Title"] = "Pagamento Confirmado";
}

<!--
    VIEW DE SUCESSO - CONFIRMAÇÃO DE PAGAMENTO

    Esta página é exibida quando o cliente confirma o pagamento com sucesso via email.

    DADOS RECEBIDOS VIA VIEWBAG:
    - ViewBag.OrderNumber: Número do pedido (ex: PED-2025-000001)
    - ViewBag.TotalAmount: Valor total do pedido
    - ViewBag.CustomerName: Nome do cliente
-->

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">

            <!-- Ícone de Sucesso -->
            <div class="text-center mb-4">
                <div class="mb-3">
                    <i class="bi bi-check-circle-fill text-success" style="font-size: 6rem;"></i>
                </div>
                <h1 class="display-4 mb-3">@ViewBag.SuccessTitle</h1>
                <p class="lead text-muted">Olá @ViewBag.CustomerName, seu pagamento foi confirmado com sucesso!</p>
            </div>

            <!-- Card com Informações -->
            <div class="card shadow-sm mb-4">
                <div class="card-body p-4">
                    <div class="row g-4">
                        <div class="col-md-6">
                            <h6 class="text-muted mb-2">
                                <i class="bi bi-receipt me-2"></i>Número do Pedido
                            </h6>
                            <p class="h5 mb-0">@ViewBag.OrderNumber</p>
                        </div>
                        <div class="col-md-6">
                            <h6 class="text-muted mb-2">
                                <i class="bi bi-cash-coin me-2"></i>Valor Total
                            </h6>
                            <p class="h5 mb-0 text-success">@ViewBag.TotalAmount.ToString("C", new System.Globalization.CultureInfo("pt-BR"))</p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Próximos Passos -->
            <div class="card bg-light mb-4">
                <div class="card-body">
                    <h5 class="card-title mb-3">
                        <i class="bi bi-list-check me-2"></i>Próximos Passos
                    </h5>
                    <ol class="mb-0">
                        <li class="mb-2">Seu pedido já está sendo processado e será enviado em breve</li>
                        <li class="mb-2">Os produtos foram reservados e o estoque foi atualizado</li>
                        <li class="mb-2">Você receberá um email com o código de rastreamento quando o pedido for enviado</li>
                        <li class="mb-0">Acompanhe o status do seu pedido na área "Meus Pedidos"</li>
                    </ol>
                </div>
            </div>

            <!-- Botões de Ação -->
            <div class="d-flex gap-3 justify-content-center">
                <a href="@Url.Action("Index", "Home")" class="btn btn-outline-dark btn-lg">
                    <i class="bi bi-house-door me-2"></i>Voltar ao Início
                </a>
                <a href="@Url.Action("Index", "Order")" class="btn btn-dark btn-lg">
                    <i class="bi bi-box-seam me-2"></i>Meus Pedidos
                </a>
            </div>

        </div>
    </div>
</div>
